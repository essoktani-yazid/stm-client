package com.smarttask.model;

/**
 * Type of planner item for visual differentiation
 */
public enum PlannerItemType {
    TASK,    // Private task (user-specific)
    EVENT    // Public event (visible to all)
}
